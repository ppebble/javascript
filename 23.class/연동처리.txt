연동처리 :: 자식클래스와 부모클래스

자식클래스에서 부모클래스의 생성자를 호출해야하는 경우에 발생한다.super()

    super ? 부모와 자식 클래스를 구분하기 위해 사용함
    
    toString() :: 문자열반환 메소드

    부모클래스가 없는 경우 Object클래스를 상속받게된다.(=모든 클래스는 Object클래스의 자식클래스)
    Obejct클래스의 constructor 프로퍼티는 알고있어야함.

    MyClass.prototype =new Object() ; =>안써도 자동으로 추가되는 구문임

    상속 구현 시 부모 클래스의 생성자에 매개변수가 있는 경우 자식클래스에서 부모클래스의 생성자를 호출 할 때는
    매개변수 값을 전달해야한다.

    2.JavaScript에서 부모생성자 호출
    function Child(param1...){
        Parent.call(this[param1...]);
    }
    -부모클래스의 생성자를 호출할 때 함수객체에서 제공하는 call()메소드를 통해 첫 번째 매개변수에
    자식 인스턴스를 전달하고, 두 번째 매개변수 이후부터는 부모의 생성자로 전달할 데이터를 전달해준다.


◈항상 부모의 인스턴스가 만들어지고 난 후 자식클래스의 인스턴스가 부모클래스의 인스턴스에 추가됨을 잊으면 안된다.